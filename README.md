# Fork of TylerBoni/FlipMouse

This fork makes the mouse toggling more stable when the phone is under heavy use, like when operating a browser
- It adds a keymap to the module to remove the "Favorites" mapping from the Favorites button
- It reverts the distinction between a short press and a long press
